import React from "react";
import podcastPostPageStyles from "./PodcastPostPage.module.css";

const PodcastPostPage = ({ slug }) => {
  console.log(slug);
  return <div>PodcastPostPage</div>;
};

export default PodcastPostPage;
